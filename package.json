{
  "name": "viewroute",
  "version": "0.2.0",
  "description": "a view router for morphable",
  "main": "build.js",
  "dependencies": {
    "morphable": "^0.5.4"
  },
  "devDependencies": {
    "bel": "^5.1.7",
    "babel-core": "^6.26.0",
    "babel-preset-env": "^1.6.1",
    "babelify": "^8.0.0"
  },
  "scripts": {
    "build": "babel index.js --presets=env --out-file build.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT"
}
